import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'payroll-checker-frontend/',
  webDir: 'dist'
};

export default config;
